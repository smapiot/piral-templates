<% if (standalone) { -%>
import { fromSolid } from 'piral-solid/convert';
<% } -%>
import { lazy } from 'solid-js';

const Page = lazy(() => import('./Page'));

export function setup(app) {
<% if (standalone) { -%>
  app.registerPage('/sample', app.fromSolid(Page));
<% } else { -%>
  app.registerPage('/sample', fromSolid(Page));
<% } -%>
}
