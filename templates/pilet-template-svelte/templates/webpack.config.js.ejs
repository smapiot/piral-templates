const extendWebpack = require('piral-svelte/extend-webpack');

module.exports = (config) => {
  return extendWebpack({
    emitCss: false,
    compilerOptions: {
      css: false,
    },
  })(config);
};
